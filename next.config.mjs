/**
 * Next.js 설정 파일
 * 
 * 현재는 기본 설정만 사용 중입니다.
 * 향후 이미지 최적화, 환경 변수, 리다이렉트 등의 설정을 추가할 수 있습니다.
 * 
 * @file next.config.mjs
 */

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
